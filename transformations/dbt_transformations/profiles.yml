dbt_transformations:
  target: prod
  outputs:
    prod:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "{{ env_var('GCP_BQ_LOCATION') }}"
      threads: 4
      location: "{{ env_var('GCP_BQ_LOCATION') }}"